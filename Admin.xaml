<Window x:Class="MDK.Admin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Панель администратора" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Window.Resources>
        <Style x:Key="SecondaryBackground" TargetType="Border">
            <Setter Property="Background" Value="#BBDCFA"/>
        </Style>

        <Style x:Key="ExitButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#0C4882"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#0C4882"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EAF4FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="Foreground" Value="#0C4882"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="NormalText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>

        <Style x:Key="BoldText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="#0C4882"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0A3A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BBDCFA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="12,6,12,6"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.15" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatsText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="Foreground" Value="#0C4882"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="FontFamily" Value="Bahnschrift Light SemiCondensed"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#F0F8FF"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Style="{StaticResource SecondaryBackground}" Padding="20,15">
            <Grid>
                <TextBlock Text="Панель администратора" Style="{StaticResource HeaderText}" HorizontalAlignment="Center"/>
                <Button x:Name="BtnExit" Content="Выход" Style="{StaticResource ExitButton}" HorizontalAlignment="Right" Click="BtnExit_Click"/>
            </Grid>
        </Border>

        <!-- Контент -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Статистика -->

                <!-- Управление пользователями -->
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource SectionBorder}" Margin="10,10,-3,10">
                    <StackPanel>
                        <TextBlock Text="Пользователи" Style="{StaticResource HeaderText}"/>
                        <ListView x:Name="LvUsers" Height="150">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Логин" Width="120" DisplayMemberBinding="{Binding login}"/>
                                    <GridViewColumn Header="Роль" Width="100" DisplayMemberBinding="{Binding role.name}"/>
                                    <GridViewColumn Header="Действия" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Изменить" Style="{StaticResource AccentButton}" Padding="5,2" Click="BtnEditUser_Click"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button x:Name="BtnAddUser" Content="Добавить пользователя" Style="{StaticResource AccentButton}" Click="BtnAddUser_Click"/>
                            <Button x:Name="BtnRefreshUsers" Content="Обновить" Style="{StaticResource AccentButton}" Click="BtnRefreshUsers_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Управление партнерами -->
                <Border Grid.Row="1" Style="{StaticResource SectionBorder}" Margin="10,10,487,10" Grid.ColumnSpan="2">
                    <StackPanel>
                        <TextBlock Text="Партнеры" Style="{StaticResource HeaderText}"/>
                        <ListView x:Name="LvPartners" Height="150">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Название" Width="150" DisplayMemberBinding="{Binding name}"/>
                                    <GridViewColumn Header="Тип" Width="100" DisplayMemberBinding="{Binding type_partner_.name}"/>
                                    <GridViewColumn Header="Рейтинг" Width="80" DisplayMemberBinding="{Binding reiting}"/>
                                    <GridViewColumn Header="Действия" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Изменить" Style="{StaticResource AccentButton}" Padding="5,2" Click="BtnEditPartner_Click"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button x:Name="BtnAddPartner" Content="Добавить партнера" Style="{StaticResource AccentButton}" Click="BtnAddPartner_Click"/>
                            <Button x:Name="BtnRefreshPartners" Content="Обновить" Style="{StaticResource AccentButton}" Click="BtnRefreshPartners_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Последние заказы -->
                <Border Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource SectionBorder}" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="Последние заказы" Style="{StaticResource HeaderText}"/>
                        <ListView x:Name="LvRecentOrders" Height="200">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Стоимость" Width="120" DisplayMemberBinding="{Binding TotalCost, StringFormat={}{0:N2} ₽}"/>
                                    <GridViewColumn Header="Действия" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Просмотр" Style="{StaticResource AccentButton}" Padding="5,2" Click="BtnViewOrder_Click"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        <Button x:Name="BtnRefreshOrders" Content="Обновить заказы" Style="{StaticResource AccentButton}" HorizontalAlignment="Center" Margin="0,10,0,0" Click="BtnRefreshOrders_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>